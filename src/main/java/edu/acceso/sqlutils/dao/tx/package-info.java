/**
 * Paquete con clases y listeners que amplian la funcionalidad del gestor de transacciones.
 * Se incluye solamente la capacidad de agregar una caché de entidades por transacción,
 * que se une a la de diferir registros que ya se implementa en el paquete principal {@code edu.acceso.sqlutils.tx}.
 */
package edu.acceso.sqlutils.dao.tx;