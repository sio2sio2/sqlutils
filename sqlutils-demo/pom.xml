<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
       <groupId>edu.acceso.sqlutils</groupId>
       <artifactId>sqlutils</artifactId>
       <version>${revision}</version>
       <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>sqlutils-demo</artifactId>
    <name>sqlutils-demo</name>
    <description>Demo de sqlutils</description>
    <packaging>jar</packaging>

    <properties>
        <hibernate.version>7.3.0.Final</hibernate.version>
    </properties>

    <build>
        <plugins>
            <!-- Para este módulo no queremos generar documentación, porque sólo es código de pruebas -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.5.18</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
            <version>4.7.7</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.53.1.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>edu.acceso.sqlutils</groupId>
            <artifactId>sqlutils-jdbc</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>edu.acceso.sqlutils</groupId>
            <artifactId>sqlutils-hikaricp</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>edu.acceso.sqlutils</groupId>
            <artifactId>sqlutils-orm</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>edu.acceso.sqlutils</groupId>
            <artifactId>sqlutils-jpa</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Especificación JPA -->
        <dependency>
            <groupId>jakarta.persistence</groupId>
            <artifactId>jakarta.persistence-api</artifactId>
            <version>3.2.0</version>
        </dependency>

        <!-- Implementación de Hibernate para la especificación anterior -->
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-core</artifactId>
            <version>${hibernate.version}</version>
            <scope>runtime</scope>
        </dependency>

      <!-- Soporte en Hibernate para SQLite -->
      <dependency>
          <groupId>org.hibernate.orm</groupId>
          <artifactId>hibernate-community-dialects</artifactId>
          <version>${hibernate.version}</version>
          <scope>runtime</scope>
      </dependency>

      <!-- Opcional para anotaciones de comprobación -->
      <dependency>
          <groupId>jakarta.validation</groupId>
          <artifactId>jakarta.validation-api</artifactId>
          <version>3.1.1</version>
      </dependency>

      <!-- Implementación de Hibernate para la especificación anterior -->
      <dependency>
          <groupId>org.hibernate.validator</groupId>
          <artifactId>hibernate-validator</artifactId>
          <version>9.1.0.Final</version>
          <scope>runtime</scope>
      </dependency>

      <!-- Opcional para evaluación dinámica de cadenas de texto (puede necesitarlo Hibernate Validator) -->
      <dependency>
          <groupId>org.glassfish.expressly</groupId>
          <artifactId>expressly</artifactId>
          <version>6.0.0</version>
          <scope>runtime</scope>
      </dependency>
    </dependencies>

</project>
